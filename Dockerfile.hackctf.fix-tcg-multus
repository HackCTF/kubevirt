FROM harbor.k8s.local/library/virt-launcher:fix-tcg-multus-v1

# Replace virt-launcher binary with version that includes:
# 1. Pre-start hook to wait for secondary network interfaces (fix-tcg-multus-v1)
# 2. Auto-generate cloud-init network config for secondary interfaces (NEW in v2)
COPY _out/virt-launcher /usr/bin/virt-launcher

# Default command matches the original image
CMD ["virt-launcher"]
